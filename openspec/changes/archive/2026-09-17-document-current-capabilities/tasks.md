# Tasks

## 1. Capture the Brownfield Baseline

- [x] 1.1 Derive the stable generic blob-store capability inventory from current documentation and tests, and verify no consumer-domain model is introduced.
- [x] 1.2 Write behavioral delta specs for the six MeldStore capabilities and verify they preserve integrity, lifecycle, backend, and qualification boundaries.

## 2. Integrate the Baseline

- [x] 2.1 Validate the complete change with OpenSpec strict validation and verify every artifact is ready.
- [x] 2.2 Sync the validated deltas into main specs, archive the documentation-only change, and verify the resulting OpenSpec root is healthy.
